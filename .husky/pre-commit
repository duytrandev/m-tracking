
#!/bin/bash
# husky pre-commit hook

echo "🔍 Running pre-commit checks..."

# Stage 1: Fast linting and formatting (immediate feedback)
echo "\n📝 Running lint-staged on staged files..."
pnpm lint-staged || {
  echo "❌ Lint-staged checks failed. Please fix the issues above."
  exit 1
}

echo "✅ Pre-commit checks passed! (Type checking will run on push)\n"
